{
  "name": "owldrop-site",
  "private": true,
  "scripts": {
    "deploy": "./scripts/deploy.sh",
    "audit": "npm audit --audit-level=high"
  },
  "devDependencies": {
    "wrangler": "4.120.0"
  }
}
